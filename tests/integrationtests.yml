http.bind.port: 18080

sources:
  - id: itest
    type: log
    file_pattern: tests/demo\.log(\.(?P<rotation>\d)(\.gz)?)?
    line_pattern: "%{TIMESTAMP_ISO8601:timestamp} %{GREEDYDATA:message}"
    datetime_pattern: "%Y-%m-%d %H:%M:%S"
